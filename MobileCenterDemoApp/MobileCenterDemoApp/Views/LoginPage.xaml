<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MobileCenterDemoApp.ViewModels;assembly=MobileCenterDemoApp.Android"
             x:Class="MobileCenterDemoApp.Views.LoginPage"
             Navigation="{Binding Navigation}">
    <ContentPage.BindingContext>
        <viewModels:LoginViewModel />
    </ContentPage.BindingContext>
    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="Center">
        <Label Text="{Binding Title}" HorizontalOptions="Center" VerticalOptions="Start"/>
        <Label TextColor="Red" Text="{Binding ErrorMessage}" IsVisible="{Binding ErrorNotExists}" HorizontalOptions="Center"/>
        <Button Command="{Binding LoginViaFacebookCommand}" Text="Login via Facebook" HorizontalOptions="EndAndExpand"/>
        <Button Command="{Binding LoginViaTwitterCommand}" Text="Login via Twitter" HorizontalOptions="EndAndExpand"/>
    </StackLayout>
</ContentPage>